FINAL VISUAL VERSION

This version uses the EXACT user-provided:
- logo.png
- hero.jpg

They are placed in the same directory as index.html and referenced directly:
src="logo.png"
background-image:url("hero.jpg")

The hero image is shown as the actual full background with a subtle readable overlay.
The logo is displayed in the header.

Existing application functionality was preserved from the supplied project.
